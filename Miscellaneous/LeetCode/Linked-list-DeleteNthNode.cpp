#include <bits/stdc++.h>

using namespace std;

struct ListNode{
	int val;
	ListNode * next;

	ListNode(int value) {
		val = value;
		next = NULL;
	}
};

ListNode* removeNthFromEnd(ListNode* head, int n) {
    ListNode * current = head, * first, * second;
    while (current != NULL) {

        first->
    }
}
