#include <bits/stdc++.h>

using namespace std;


string convert(string s, int numRows) {
	vector<vector<char>> rows(numRows);
	bool up = false;
	int n = s.length();

	for (int i = 0; i < n; i++) {

	}
}
